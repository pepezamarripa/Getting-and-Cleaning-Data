---
title: "R Notebook"
output:
  html_document:
    df_print: paged
---

##Objective of the projct\
1. Clean a tidy data set\
2. Create a repository in Github that keeps the code in R that performs the analysis, the code book and the README:\
- run_analysis.R: performs the data preparation and then followed by the 5 steps required as described in the course project’s definition:\
  (a) Merges the training and the test sets to create one data set.\
  (b) Extracts only the measurements on the mean and standard deviation for each measurement.\
  (c) Uses descriptive activity names to name the activities in the data set\
  (d) Appropriately labels the data set with descriptive variable names.\
  (e) From the data set in step 4, creates a second, independent tidy data set with the average of each variable for each activity and each subject.\
\
- Code_book.Rmd: a code book that describes the dataset\
- final_Dataset.txt: exported txt file with the tidy data set